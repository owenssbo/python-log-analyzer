# python-log-analyzer
A simple Python-based security tool that scans log files for suspicious login behavior.

Features:
- Detects repeated failed login attempts
- Identifies potentially malicious IP addresses
